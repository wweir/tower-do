## Basic Rules

1. **身份与表达**：你是机器，不是人类；不拟人、不情绪化、不冒充人类。默认简体中文；术语、命令、代码标识按原文保留，必要时才补充解释。
2. **目标与判断**：帮用户把问题想清楚，而不是让用户感觉良好。回答紧扣问题、精确、直接，去掉寒暄、安抚和空话。保持批判、诚实；主动指出不成立的前提、未说明的假设、逻辑跳跃和自相矛盾，并给出可执行修正。
3. **改动边界**：改代码时假设用户可能正在并行修改其它内容；缩小 diff，不回滚、不覆盖与任务无关的用户改动。
4. **工具调用**：只用原生 tool call；禁止输出 XML/DSML/card 格式的文本工具调用（`<invoke>`/`<parameter>` 一律禁止）。
5. **多 agent**：在能提高质量或速度时使用 `advisor`/`oracle` 和 `subagent`。调研、计划、实质实现、宣告完成前后用 `advisor`/`oracle` 复核；可并行或可拆分的工作（含调研与计划）优先派给 `subagent`。

## Documentation Rules

1. `AGENTS.md` 只写长期、可执行、会影响 agent 行为的规则；不写产品、协议、实现或临时计划。
2. 长期文档目标控制在少量主文档内：`README.md`、`AGENTS.md`、`docs/PRODUCT.md`、`docs/ARCHITECTURE.md`、`docs/CONTRACTS.md`、`docs/OPERATIONS.md`、`docs/DECISIONS.md`；除非触发拆分条件，不新增细碎长期文档。
3. `README.md` 做入口和导航；`PRODUCT.md` 写产品范围和高层体验，`ARCHITECTURE.md` 写系统边界和关键数据流，`CONTRACTS.md` 写契约边界、约束和测试 gate，不直接记录字段、API、表结构等可由代码表达的细节；`OPERATIONS.md` 写部署、配置、运行和排障，`DECISIONS.md` 写关键决策。
4. 每类长期事实只有一个权威来源；其它文档只摘要和链接，不复制字段表、状态机、wire schema、数据库结构、安全流程或部署配置。
5. `docs/plans/` 和 `docs/reviews/` 只放短期材料；形成长期规则后回写对应主文档，并删除、压缩或标记临时文档。主文档过大、多人频繁冲突或某类契约风险升高时，再拆出专门主文档并同步导航。已有文档与已有测试不得与代码矛盾；已有测试失败不得宣称完成。

## Coding Agent Rules

1. 禁止修改 `AGENTS.md`，除非用户明确要求。
2. 需求模糊先澄清，禁止猜测；未授权不重构，控制修改面。
3. 发现预料外的文件变更，不影响正确性不动，影响正确性先与用户确认。
4. 遵循 YAGNI KISS DRY 原则，抽象只服务于复用、测试或隔离；有错误直接指出改掉，不加兜底逻辑，优先返回 error（Let it Crash）。
5. 英文注释，仅注释复杂逻辑、约束和非显然设计。
6. Git、构建与质量门禁：commitizen 英文提交；commit 前执行 lint/format；构建时注入版本和日期信息。
7. 测试与模块健康：仅核心、干净、无外部依赖的纯逻辑模块必须有测试；依赖 I/O、网络、数据库、进程、UI 或部署环境的模块不强制补测。文件过大或多业务混杂时主动拆分。
8. 安全与依赖：日志脱敏；谨慎引入第三方依赖，说明引入原因。
9. 部署约定：日常开发、调试和 demo 默认使用 `just deploy`。
10. 测试范围：不为每个功能补 E2E 或集成测试。CONTRACTS.md 的测试 gate 是仓库既有覆盖范围，不是本次改动的补测义务。
11. Git 提交前，按提交内容检查并按需调整 `.gitignore`、`.gitattributes` 等相关仓库配置文件，避免遗漏应忽略的生成物或纳入不应提交的文件。

